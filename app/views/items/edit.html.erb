<div class="text-center form-center">
  <%= image_tag "icons/wrapped-gift.png", alt: "", class: "mb-4", height: "57px" %>
  <h1 class="mb-4">Edit item</h1>
  
  <%= render 'form', item: @item %>

  <%= button_to 'Delete this item', item_path(@item), method: :delete, data: { confirm: "This item will be deleted and its irreversible. Are you sure?" }, class: "w-100 btn btn-lg btn-outline-danger mt-4" %>
</div>
